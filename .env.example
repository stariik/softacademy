# Database
DATABASE_URL="postgresql://username:password@localhost:5432/softacademy?schema=public"

# Auth
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Note: SMS (Twilio) and Email (SMTP) credentials are configured in the admin panel
# Go to Admin Panel > Settings to configure them
